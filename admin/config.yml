# config.yml

backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)
media_folder: 'assets/uploads'
collections:
  - name: 'includes'
    label: 'includes'
    folder: '_includes/'
    fields:
      - { name: Title }