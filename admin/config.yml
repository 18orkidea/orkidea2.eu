# config.yml

backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)
media_folder: 'assets/uploads'
public_folder: '/images/uploads'
logo_url: https://your-site.com/images/logo/logo.svg
locale: 'es'
collections:
  - name: 'services'
    label: 'services'
    folder: '_services/'
    fields:
      - { titulo: Titulo, 
           descripcion: Descripción }